// src/config/wotd.js — Configuración del comando /wotd (Word of the Day)
// Copia este archivo como wotd.js y rellena los IDs.
module.exports = {
  // IDs de los canales donde se enviará la imagen del día
  canales: {
    spanish: '',  // Canal donde aprenden español
    english: '',  // Canal donde aprenden inglés
  },

  // IDs de los roles que se mencionarán en cada canal
  roles: {
    spanish: '',  // Rol para estudiantes de español
    english: '',  // Rol para estudiantes de inglés
  },
};
